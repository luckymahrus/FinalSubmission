<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@drawable/border"
    android:padding="@dimen/activity_vertical_margin"
    android:layout_margin="5dp">

    <ImageView
        android:id="@+id/iv_brand_logo"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:background="@drawable/border"
        android:padding="10dp"
        android:layout_marginEnd="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        tools:src="@android:color/darker_gray" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@id/iv_brand_logo"
        android:layout_toRightOf="@id/iv_brand_logo"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_brand_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="20dp"
            android:textSize="30sp"
            android:textStyle="bold"
            tools:text="@string/txt_tv_brand_name" />

        <TextView
            android:id="@+id/tv_brand_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/tv_brand_name"
            android:layout_toRightOf="@id/tv_brand_name"
            android:cursorVisible="false"
            android:visibility="invisible"
            tools:text="@string/txt_tv_brand_id"
            tools:visibility="invisible" />
    </LinearLayout>
</RelativeLayout>